/**
 * mapbox — shared map configuration + geocoding helper.
 * STATUS: stub (M4 — mapbox deps not installed yet, ADR-006)
 *
 * What goes here (M4): NEXT_PUBLIC_MAPBOX_TOKEN access (with a clear error
 * when missing), shared Map defaults (style URL — pick one that suits the
 * Lino aesthetic, default zoom, attribution), forward-geocode helper for
 * MapSearchBox, and the MapSigil marker-data type used by WorldMap.
 *
 * @see docs/features/map.md
 */
export {};
