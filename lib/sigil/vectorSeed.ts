/**
 * vectorSeed — lay letterform vectors onto the draw canvas.
 * STATUS: stub (M2)
 *
 * What goes here (M2): given SvgVector rows (from POST /api/vectors) and a
 * Fabric canvas, place each letterform as a manipulable object — scaled to
 * a sensible starting size, scattered/arranged so they don't stack, ready
 * for the user to weave into a sigil. Pure layout math here; Fabric calls
 * stay in DrawSigilCanvas (this module stays unit-testable).
 *
 * v1 reference: vector loading inside main:src/components/.../MakeSigilComponents/DrawSigil.tsx
 * @see docs/features/make-sigil.md
 */
export {};
