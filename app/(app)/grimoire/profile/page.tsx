/**
 * Profile — the user's stats page (left grimoire page).
 * STATUS: stub
 * Route: /grimoire/profile
 *
 * What goes here (M3): server component — avatar + username, lifetime
 * sigilCount / destroyCount, home location, SigiFriends preview (mutuals,
 * links to /grimoire/friends), <SlotMeter> (slots used / limit).
 *
 * v1 reference: git show main:src/components/.../Profile/UserProfile.tsx
 * @see docs/features/grimoire.md, docs/features/social.md
 */
export default function ProfilePage() {
  return <main className="p-8">profile (stub)</main>;
}
