/**
 * SigilMarker — one sigil on the WorldMap.
 * STATUS: stub · "use client" (M4)
 *
 * Props (planned): { sigil: MapSigil; onOpen: (id) => void }
 * Small sigil-image marker (charged glow tint); tap → popup/sheet with name,
 * owner, location and <VotePanel>.
 *
 * v1 reference: marker + popup logic inside main:src/components/.../Map/MapBox.tsx
 * @see docs/features/map.md
 */
export function SigilMarker() {
  return null;
}
