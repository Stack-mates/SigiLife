/**
 * MapSearchBox — location search / geocoder input.
 * STATUS: stub · "use client" (M4)
 *
 * Props (planned): { onSelect: ({lat, lng, name}) => void; placeholder? }
 * Mapbox Search JS (or geocoding API via lib/mapbox helper). Used by:
 * place-sigil page, StyleSigil location field, ProfileForm home location.
 *
 * v1 reference: git show main:src/components/.../Map/MapSearchBox.tsx
 * @see docs/features/map.md
 */
export function MapSearchBox() {
  return <div>MapSearchBox (stub)</div>;
}
