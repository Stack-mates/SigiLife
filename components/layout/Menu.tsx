/**
 * Menu — the global hamburger nav, mounted by (app)/layout.
 * STATUS: stub · "use client" when implemented
 *
 * What goes here (M1): top-corner toggle → glass panel with links: The Office
 * (/home), Grimoire, Map, Make Sigil, Premium, Settings; sign-out (Auth.js
 * signOut()). Current-route highlight. Collapsed by default on ritual pages
 * (charge/destroy) so it never breaks immersion.
 *
 * v1 reference: git show main:src/components/Parts/Menu.tsx
 * @see docs/COMPONENT_MAP.md
 */
export function Menu() {
  return null;
}
