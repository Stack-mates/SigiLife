/**
 * GhostCursor — red trailing cursor/touch effect in the destroy ritual.
 * STATUS: stub · "use client"
 *
 * Canvas-based pointer trail (v1: red ghost trail). Port from v1; make the
 * trail color a prop (emotion color), support touch, clean up listeners.
 *
 * v1 reference: git show main:src/components/.../DestroyComponents/GhostCursor.tsx
 * @see docs/features/charge-destroy.md
 */
export function GhostCursor() {
  return null;
}
