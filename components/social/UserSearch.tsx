/**
 * UserSearch — find users by username.
 * STATUS: stub · "use client" (M3)
 *
 * Debounced input → GET /api/users?q= → result rows (avatar, username,
 * <FollowButton>). Min 2 chars; empty/no-results states in-fiction
 * ("the mirror shows no one by that name").
 *
 * v1 reference: search section of main:src/components/.../Profile/UserFriends.tsx
 * @see docs/features/social.md
 */
export function UserSearch() {
  return <div>UserSearch (stub)</div>;
}
