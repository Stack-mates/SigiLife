/**
 * SigilThumb — library grid thumbnail.
 * STATUS: stub · server component
 *
 * Props (planned): { sigil: SigilSummary; href: string } — href varies by
 * library mode (detail page vs pick-mode ritual routes).
 * Shows: imageData render, name, ⚡ if charged (tinted by chargedEmotion),
 * subtle destroyed treatment in the completed view.
 *
 * v1 reference: git show main:src/components/.../SigiLibrary/SigilThumb.tsx
 * @see docs/features/grimoire.md
 */
export function SigilThumb() {
  return <div>SigilThumb (stub)</div>;
}
