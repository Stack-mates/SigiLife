/**
 * SlotMeter — sigil slots used / available.
 * STATUS: stub · server component (M7; a basic 12-cap version can ship M2)
 *
 * Props (planned): { used: number; limit: number }
 * Shown on profile, in the make-sigil wizard when near cap, and on /premium.
 * Near/at cap: nudge copy linking to destroy (free a slot) or /premium.
 *
 * @see docs/features/monetization.md
 */
export function SlotMeter() {
  return <div>SlotMeter (stub)</div>;
}
